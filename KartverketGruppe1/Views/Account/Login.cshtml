@model LoginViewModel

@{
    ViewData["Title"] = "Log in";
}
@* <h2>Logg inn</h2>

<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>

            <div class="form-group">
                <label asp-for="Email">E-post</label>
                <input asp-for="Email" class="form-control" />
                <span asp-validation-for="Email" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label asp-for="Password">Passord</label>
                <input asp-for="Password" class="form-control" type="password" />
                <span asp-validation-for="Password" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label>
                    <input asp-for="RememberMe" type="checkbox" />
                    Husk meg
                </label>
            </div>

            <button type="submit" class="btn btn-primary">Logg inn</button>
        </form>
        <p>Har du ikke bruker? <a href="/Account/Register">Registrer deg her</a></p>
    </div>
</div> *@
<style>
    body {
        background-size: cover;
        background-position: center;
        font-family: Arial, sans-serif;
    }

    .login-container {
        background-color: rgba(255, 255, 255, 0.8);
        border-radius: 8px;
        padding: 20px;
        width: 320px;
        position: absolute;
        top: 375px; /*Endret fra 55% til 375px*/
        left: 50%;
        transform: translate(-50%, -50%);
        box-shadow: 0 0 10px rgba(0,0,0,0.1);
    }

        .login-container h2 {
            margin-bottom: 10px;
            color: #333;
        }

    .form-group {
        margin-bottom: 15px;
    }

    .form-control {
        width: 100%;
        padding: 10px;
        border: 1px solid #ddd;
        border-radius: 4px;
        box-sizing: border-box;
    }

    .btn-block {
        width: 100%;
        padding: 10px;
        margin-bottom: 10px;
        border: none;
        border-radius: 4px;
        cursor: pointer;
        font-weight: bold;
    }

    .btn-primary {
        background-color: #8EBF94;
        color: white;
        width: 100%;
        height: 45px;
        text-align: center; 
        border: 1px solid #ddd;
        border-radius: 5px;
    }

    .btn-primary:hover{
            background-color: #58a77b;
    }

    .separator {
        text-align: center;
        margin: 15px 0;
        color: #777;
    }

    .btn-google, .btn-facebook, .btn-apple {
        text-align: left;
        padding-left: 45px;
        position: relative;
    }

    .btn-google {
        background-color: #fff;
        color: #757575;
        border: 1px solid #ddd;
    }

    .btn-facebook {
        background-color: #3b5998;
        color: #fff;
    }

    .btn-apple {
        background-color: #000;
        color: #fff;
    }

        .btn-google img, .btn-facebook img, .btn-apple img {
            position: absolute;
            left: 10px;
            top: 50%;
            transform: translateY(-50%);
            width: 20px;
            height: 20px;
        }

    .register-link, .guest-link {
        color: #4CAF50;
        text-decoration: none;
    }

    .guest-link {
        display: block;
        margin-bottom: 15px;
        color: #4CAF50;
    }

    .temporary-notice {
        position: fixed;
        bottom: 20px;
        left: 20px;
        background-color: rgba(255, 255, 255, 0.8);
        padding: 10px;
        border-radius: 5px;
    }
</style>

<div class="login-container">
    <h2>Logg på</h2>
    <p>Har du ikke konto? <a href="/Account/Register" class="register-link">Registrer deg</a></p>
    <a href="/Home/KartInnmelding" class="guest-link">Fortsett som gjest</a>

    <!-- <form asp-action="Index" asp-controller="Home" method="post">
        <div class="form-group">
            <input type="text" class="form-control" name="Epost" placeholder="Epost" required>
        </div>
        <div class="form-group">
            <input type="password" class="form-control" name="Passord" placeholder="Passord" required>
        </div>
        <button type="submit" class="btn btn-primary btn-block">Fortsett</button>
    </form>
                                                              -->
    <form method="post">
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
        <div class="form-group">
            <input asp-for="Email" class="form-control" placeholder="Epost" />
            <span asp-validation-for="Email" class="text-danger"></span>
        </div>

        @* <div class="form-group">
            <label asp-for="Email">E-post</label>
            <input asp-for="Email" class="form-control" />
            <span asp-validation-for="Email" class="text-danger"></span>
        </div> *@

        <div class="form-group">
            <input asp-for="Password" class="form-control" placeholder="Passord" type="password" />
            <span asp-validation-for="Password" class="text-danger"></span>
        </div>

        @* <div class="form-group">
            <label asp-for="Password">Passord</label>
            <input asp-for="Password" class="form-control" type="password" />
            <span asp-validation-for="Password" class="text-danger"></span>
        </div> *@

        <div class="form-group">
            <label>
                <input asp-for="RememberMe" type="checkbox" />
                Husk meg
            </label>
        </div>

        <button type="submit" class="btn-primary">Logg inn</button>
    </form>

    <!--                                                            -->
    <div class="separator">eller</div>

    <button class="btn btn-google btn-block" onclick="redirectToHjemSkjermGjest">
        <img src="~/images/GoogleIcon.png" alt="Google icon" /> Fortsett med Google
    </button>
    <button class="btn btn-facebook btn-block" onclick="redirectToHjemSkjermGjest">
        <img src="~/images/FacebookIcon.png" alt="Facebook icon" /> Fortsett med Facebook
    </button>
    <button class="btn btn-apple btn-block" onclick="redirectToHjemSkjermGjest">
        <img src="~/images/AppleIconWhite.png" alt="Apple icon" /> Fortsett med Apple
    </button>

</div>

<script>

    function redirectToHjemSkjermGjest() {
        window.location.href = "/Home/Privacy";
    }

    function setRandomBackground() { // Funksjon for å hente random bilde fra 'Bakgrunnsbilder'
        fetch('/Home/GetRandomBackgroundImage')
            .then(response => response.json())
            .then(data => {
                if (data.error) {
                    console.error(data.error);
                } else {
                    document.body.style.backgroundImage = `url('${data.imagePath}')`;
                }
            })
            .catch(error => console.error('Error:', error));
    }


    window.onload = setRandomBackground;
</script>